version: '3.9'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: todolist
      POSTGRES_PASSWORD: 123456
      POSTGRES_DB: todo_development
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  web:
    build: .
    depends_on:
      - db
    environment:
      RAILS_ENV: development
      DATABASE_URL: postgresql://todo:LRg4sJjcKH1E0yz98HORwhQXhDg3vr7U@dpg-d04glch5pdvs73cbiukg-a.oregon-postgres.render.com/todo_production_9zjt
      RAILS_MASTER_KEY: ${RAILS_MASTER_KEY}
    ports:
      - "3000:80"
    command: ./bin/thrust ./bin/rails server

volumes:
  pgdata:
