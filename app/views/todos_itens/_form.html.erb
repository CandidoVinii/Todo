<%= form_with(model: [@todo_list, @todo_item]) do |form| %>
  <% if @todo_item.errors.any? %>
    <div>
      <h2><%= pluralize(@todo_item.errors.count, "erro") %> encontrados:</h2>
      <ul>
        <% @todo_item.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, "Tarefa" %>
    <%= form.text_field :content %>
  </div>

  <div>
    <%= form.label :done, "Concluído?" %>
    <%= form.check_box :done %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
